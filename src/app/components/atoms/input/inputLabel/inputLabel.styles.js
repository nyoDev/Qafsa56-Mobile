import {StyleSheet} from 'react-native';

const styles = {
  labelWrapper: {
    marginVertical: 8,
  },
  labelWrapperSmall: {
    marginVertical: 4,
  },
};

const s = StyleSheet.create(styles);

export default s;
